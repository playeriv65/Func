application:
  name: MyApp
  version: 2.0
  environment: production

database:
  host: localhost
  port: 5432
  name: mydb
  
features:
  - authentication
  - logging
  - caching
  
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
