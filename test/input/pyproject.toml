[project]
name = "myproject"
version = "1.0.0"
description = "A sample project"
authors = ["Developer <dev@example.com>"]

[project.dependencies]
requests = ">=2.28.0"
pandas = ">=1.5.0"

[tool.pytest]
testpaths = ["tests"]
